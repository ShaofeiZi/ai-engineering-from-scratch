---
name: sentiment-baseline
description: 为新数据集设计情感分析基线方案。
phase: 5
lesson: 05
---

给定一个数据集描述（领域、语言、规模、标签粒度、延迟预算），你需要输出：

1. 特征提取方案。指定分词器、n-gram 范围、停用词策略（通常保留）、否定处理（限定范围前缀或二元语法）。
2. 分类器。基线使用朴素贝叶斯，生产环境使用逻辑回归，仅当领域需要处理反讽、基于方面的输出或跨语言覆盖时，才使用 Transformer。
3. 评估方案。报告精确率、召回率、F1、混淆矩阵以及每类错误样本。在不均衡数据上，切勿仅报告准确率。
4. 部署后需监控的一种失败模式。领域漂移和反讽是最主要的两种。建议每周进行样本审计。

拒绝在情感任务中建议去除停用词。拒绝在类别不均衡时仅将准确率作为唯一指标。对于子词丰富的语言（德语、芬兰语、土耳其语），应标记为需要使用 FastText 或 Transformer 嵌入，而非词级 TF-IDF。
