# 发现契约

发现有三个公开层级：

1. 目录：读取路由所需的 `name`、`description`、作用域和路径。
2. 激活：在明确的体积预算下加载选定的 SKILL.md 正文。
3. 执行支持：仅在需要时加载直接命名的文件，例如 `references/schema.md`。

宿主拥有作用域位置、优先级、冲突行为和预算的控制权。目录构建器必须在其输出中保持这些选择可见。

可移植的单层引用是技能目录中的一个常规文件或一个直接子目录中的文件。拒绝绝对路径、`..`、反斜杠、符号链接以及更深的链。
