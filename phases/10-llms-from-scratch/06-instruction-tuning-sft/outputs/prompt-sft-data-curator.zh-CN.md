---
name: prompt-sft-data-curator
description: 设计并整理用于监督微调的指令数据集
version: 1.0.0
phase: 10
lesson: 6
tags: [sft, instruction-tuning, fine-tuning, data-curation, alignment]
---

# SFT 数据策展器

在为特定能力(代码生成、数学、对话、安全)设计指令微调数据集时,使用此框架规划数据收集、定义质量标准并构建训练流水线。

## 输入要求

请提供:
- **目标能力**(例如"Python 代码生成"、"医疗问答"、"多轮对话")
- **基础模型**(例如 Llama 3 8B、Mistral 7B、Qwen 2.5 72B)
- **预算**(标注工时,合成生成的 API 成本)
- **格式偏好**(Alpaca、ShareGPT、ChatML)

## 步骤 1:数据集设计

### 规模指南

| 质量级别 | 所需样本数 | 预期结果 |
|--------------|----------------|------------------|
| 研究原型 | 1,000-5,000 | LIMA 级:若样本由专家撰写,可与更大规模数据集媲美 |
| 生产 v1 | 10,000-50,000 | Stanford Alpaca 级:在常见任务上有扎实的指令遵循能力 |
| 生产 v2 | 50,000-200,000 | Vicuna/Llama 2 Chat 级:稳健的多轮对话、领域覆盖 |

质量始终胜过数量。1,000 条专家撰写的样本(LIMA,2023 年 5 月)匹敌了用 50,000+ 样本训练的模型。优先考虑:

1. **多样性** -- 覆盖目标能力的完整范围
2. **准确性** -- 每条回复必须事实正确
3. **清晰性** -- 回复应简洁且结构良好
4. **难度梯度** -- 包含简单、中等和困难样本

### 多样性清单

对于通用助手:
- 开放式问题(20%)
- 事实问答(20%)
- 创意写作(10%)
- 代码生成(15%)
- 推理与数学(15%)
- 摘要(10%)
- 带约束的指令遵循(10%)

针对领域专用模型调整百分比。代码助手可能将 60% 分配给代码生成,20% 分配给代码解释。

## 步骤 2:数据格式

### Alpaca 格式(单轮)

```json
{
  "instruction": "Write a function that reverses a string in Python.",
  "input": "",
  "output": "def reverse_string(s):\n    return s[::-1]"
}
```

适用场景:单轮任务、简单的指令-响应对、快速原型验证。

### ShareGPT 格式(多轮)

```json
{
  "conversations": [
    {"from": "system", "value": "You are a Python expert."},
    {"from": "human", "value": "How do I reverse a string?"},
    {"from": "gpt", "value": "Use slicing: s[::-1]"},
    {"from": "human", "value": "What about for a list?"},
    {"from": "gpt", "value": "Same syntax works: my_list[::-1]"}
  ]
}
```

适用场景:对话应用、多轮上下文重要的场景。

### ChatML 格式(含特殊 token)

```
<|im_start|>system
You are a Python expert.<|im_end|>
<|im_start|>user
How do I reverse a string?<|im_end|>
<|im_start|>assistant
Use slicing: s[::-1]<|im_end|>
```

适用场景:目标模型原生使用 ChatML(Qwen、Yi)。

## 步骤 3:质量标准

### 单样本检查

1. **回复相关性**:回复是否真正回答了指令?
2. **事实准确性**:所有陈述是否可验证且正确?
3. **完整性**:回复是否完整地回应了指令?
4. **简洁性**:同样的信息能否用更少的词表达?
5. **格式一致性**:回复是否遵循预期风格?

### 危险信号(拒绝该样本)

- 回复自相矛盾
- 回复包含有害内容且未拒绝
- 回复捏造事实或引用
- 指令含糊且回复未澄清
- 回复只是对指令的改写复制

### 数据集级检查

- 来自任何单一来源/模板的样本不超过 5%
- 至少 80% 的回复 token 是有意义的(非填充)
- 平均回复长度为 50-200 tokens(避免过短或过长)
- 系统提示多样性:至少有 10 个不同的系统提示

## 步骤 4:训练配置

| 参数 | 推荐范围 | 说明 |
|-----------|------------------|-------|
| 学习率 | 1e-5 至 5e-5 | 大模型用更低值(70B 用 1e-5,7B 用 5e-5) |
| 轮数 | 1-3 | 监控验证损失,首次上升时即停止 |
| 批大小 | 32-128 | 若 GPU 受限则用梯度累积扩展 |
| 预热 | 步数的 0-5% | 不如预训练关键 |
| 权重衰减 | 0.0-0.1 | 短时微调可选 |
| 损失掩码 | 仅回复 token | 掩码指令和系统提示 token |
| 预训练数据混合 | 2-5% | 混入原始文本以防止灾难性遗忘 |

## 步骤 5:评估流程

训练后,在以下方面评估:

1. **指令遵循率**:在测试提示中模型产生相关、完整回复的百分比
2. **遗忘分数**:在留出的通用文本语料上与基础模型相比的困惑度
3. **格式合规性**:遵循预期聊天格式的回复百分比
4. **MT-Bench 或 AlpacaEval**:指令微调模型的标准基准
5. **领域专用评估**:针对目标能力的自定义评估

### 警示信号

- 第 1 轮后验证损失上升:你在过拟合,减少轮数或增加数据
- 遗忘分数上升超过 15%:学习率过高或轮数过多
- 模型逐字复现训练样本:严重过拟合,需要更多样化数据
- 模型拒绝良性指令:安全数据过训练,重新平衡数据集
