# MCP الأساسيات: طلبات العدالة عن الجنسية JSON-RPC

> الحديثة MCP لا توجد أي ضغط يد ولا جلسة بروتوكول. يجب أن يحمل كل طلب ما يكفي من البيانات المعدنية ليتم فهمها وافق عليها ، وتوجيهها ، وإعادة تجربتها بمفردها.

**Type:** Learn
**Languages:** Python
**Prerequisites:** Phase 13, Lessons 01 through 05
**Time:** ~55 minutes

## أهداف التعلم

- التمييز MCPأساسيات الخادم من ميزات جانب العميل.
- إعداد صحيح JSON-RPC طلبات وردود 2.0 MCP `2026-07-28`.
- ربط نسخة بروتوكول، قدرات العميل، و هوية العميل لكل طلب.
- الاستخدام `server/discover` و التعامل `UnsupportedProtocolVersionError` بدون ضغط يد
- تتبع طلب واحد مستقل من التحقق من التحقق من التحقق من النتيجة الكاملة.

## المشكلة

(إنجليزية) MCP يمكن للخادم تلقي طلبين متتاليتين من عملاء مختلفين، ذوي قدرات مختلفة، على نفس العملية أو HTTP إذا كان الخادم يتذكر ما أعلن عنه الطلب السابق، فإنه يمكن أن تطبق الإذن الخطأ أو تعيد شكل الأسلاك الخطأ.

MCP `2026-07-28` يزيل هذا الغموض. قاعدة البروتوكول غير ذات الوضع. يجب على الخادم أن يقرر كيفية التعامل مع الطلب الحالي من الطلب الحالي، وليس من تاريخ الاتصال.

هذا يغير النموذج العقلي، السلسلة القديمة كانت الاتصال أولاً، الضغط الثاني، العمليات الثالثة. السلسلة الحديثة أسهل:

1. العميل يرسل طلباً لوصف نفسه
2. يقوم الخادم بتؤكيد نسخة الطلب وقدراته
3. الخادم يتعامل مع الطريقة.
4. يعيد الخادم نتيجة منخفضة أو JSON-RPC خطأ

الطلب التالي يكرر نفس العملية من الصفر.

## المفهوم

### أساسيات الخادم

MCP الخوادم تعرض ثلاثة بدائيات أساسية:

1. **الأدوات** هي أفعال يتم التحكم فيها على النموذج، والتي تم اكتشافها مع `tools/list` ويتم استدعائه `tools/call`.
2. **الموارد** هي URI-addressed البيانات التي تم اكتشافها مع `resources/list` و تم استردادها `resources/read`.
3. **الإشارات** هي نماذج قابلة للاستعمال، اكتشفت مع `prompts/list` وترجمة `prompts/get`.

تبقى الجذور، ومعينة، والقطع الأحياء في `2026-07-28` النظام التضامنية، ولكنها مسبوقة. يجب على التنفيذات الجديدة استخدام أداة صريحة أو مدخلات الموارد للجذر، APIs للاستعراض، و OpenTelemetry لا يزال الإجراء متاح من خلال طلبات متعددة رحلة جولية ، حيث يعيد الخادم طلب إدخال والعميل يعيد العملية الأصلية. JSON-RPC طلب

### JSON-RPC غلافات

MCP استخدامات JSON-RPC 2.0:

- الطلب: `{jsonrpc, id, method, params}`
- رد: `{jsonrpc, id, result}` أو `{jsonrpc, id, error}`
- الإخطار: `{jsonrpc, method, params}` بدون أي `id`

الطلب `id` يرتبط رد فعل واحد. لا يخلق جلسة بروتوكول.

### البيانات المطلوبة من الطلب

كل طلب حديث يحمل `_meta` كائن داخل `params`:

```json
{
  "jsonrpc": "2.0",
  "id": 7,
  "method": "tools/list",
  "params": {
    "_meta": {
      "io.modelcontextprotocol/protocolVersion": "2026-07-28",
      "io.modelcontextprotocol/clientCapabilities": {},
      "io.modelcontextprotocol/clientInfo": {
        "name": "course-client",
        "version": "1.0.0"
      }
    }
  }
}
```

الإصدار البروتوكول والقدرات العميل مطلوبة. يوصى بهوية العميل. إنها بيانات عرض وتحليل التحريف ذاتية الإبلاغ، وليس اعتماد أمني.

لا يجوز للخادم استنتاج أي من هذه القيم من طلب سابق، عملية الاستديو، أو HTTP الاتصال، أو رأس النقل وحده.

### النتائج الكاملة و هوية الخادم

كل نتيجة حديثة ناجحة تتضمن `resultType`. تستخدم النتيجة النهائية العادية `"complete"`يجب أن يُعرف الخوادم نفسها أيضاً في البيانات المعدنية الناتجة:

```json
{
  "jsonrpc": "2.0",
  "id": 7,
  "result": {
    "resultType": "complete",
    "tools": [],
    "ttlMs": 30000,
    "cacheScope": "public",
    "_meta": {
      "io.modelcontextprotocol/serverInfo": {
        "name": "notes-server",
        "version": "1.0.0"
      }
    }
  }
}
```

`tools/list`, `resources/list`, `prompts/list`, `resources/templates/list`, `resources/read`و `server/discover` هي نتائج قابلة للتخفيض. `ttlMs` و `cacheScope`. الاختلالات الآمنة هي `ttlMs: 0` و `cacheScope: "private"`. يجب أن يكون عناصر القائمة مرتبة تحديدية بحيث تخلق الردود المتكافئة مفاتيح الاحتفاظ المستقرة و سياق النموذج المستقيم.

### اكتشاف بدون ضغط يد

كل خادم حديث يجب أن ينفذ `server/discover`. العميل قد يطلبها قبل طريقة أخرى لاسترداد:

- `supportedVersions`
- الخادم `capabilities`
- استخدام اختياري `instructions`
- الهوية الخادم في النتيجة `_meta`
- إشارات التخزين

الاكتشاف مفيد، لكنه ليس بوابة. `tools/list` أولاً لأن هذا الطلب يحمل بالفعل نسخة بروتوكولها وقدراتها.

إذا لم يتم دعم الإصدار المطلوب ، يعود الخادم JSON-RPC الرمز `-32022` مع:

```json
{
  "requested": "2027-01-01",
  "supported": ["2026-07-28"]
}
```

يختار العميل نسخة حديثة مدعومة بشكل متبادل ويعود محاولة جديدة JSON-RPC بطلب الهوية

### دورة حياة طلب واحد

تتبع طلب حديث في هذا الترتيب:

1. الحجز الأول JSON-RPC غلاف
2. تأكيد `jsonrpc` هو `"2.0"`، `id` موجودة `method` هو سلسلة، `params` هو كائن
3. احتاج إلى موضوع سلسلة الإصدار والقدرة في `params._meta`؛ المعلومات المعدنية الملفقة أو المفقودة `-32602`.
4. في وقت HTTP الحدود، مقارنة الإصدار، والطريقة، والعناوين الاسم المطبقة مع الجسم. `-32020` حتى عندما لا يتم دعم إحدى قيم الإصدارين.
5. بعد أن يتم تأسيس المساواة، رفض نسخة متطابقة ولكن غير مدعومة مع `-32022`.
6. تحقق من القدرات المطلوبة ثم اتجه `method` وتؤكد الحجج الخاصة بالوسيلة.
7. تحديد المصداقية وافق على العملية الملموسة قبل أن يبدأ معالجها.
8. أعد نتيجة كاملة مع هوية الخادم.
9. انسى البيانات المعدلة للاتفاقية

هذا الأمر يمنع اثنين من المكونات من تفسير مكالمات مختلفة. `Mcp-Name: notes.read` بينما الأصل ينفذ `params.name: notes.delete`كما أنها تحتفظ بإدخال غير مصمم، وارتباك الرأس، وتفاوض الإصدارات، وفشل القدرة، والإذن، وفشل المعاملة كدليل واضح.

إغلاق الموقع أو HTTP لا ينتهي جلسة بروتوكول لأن MCP لا يوجد جلسة بروتوكول

### التوافق الصريح مع التراث

الإصدارات من خلال `2025-11-25` استخدام `initialize`, `notifications/initialized`، وقدرات التواصل الموسعة ، و ، على سابقة التسريب HTTPهذا السلوك لا يزال ذو صلة عندما يتحدث عميل من عصر مزدوج إلى خادم قديم

حافظ على الفترات منفصلة. يتم تحديد طلب حديث عن طريق البيانات المطلوبة لكل طلب. يتم اختيار اتصال سابق فقط من خلال مسار العودة الموثوق. لا ترسل `initialize` كالتخلفية ل `2026-07-28` الخادم

لذلك، فإن "غير الجنسية" لها معنى محدد في العصر. `2026-07-28`، هو بروتوكول غير متغير: كل طلب عادي يمكن تفسيره بشكل مستقل ولا MCP في نسخ من خلال `2025-11-25`إن إعدادات التشغيل والتحديث والإمكانيات المفروضة تنتمي إلى اتصال، لذلك قد يحافظ مُعدل التوافق على حالة الاتصال القديمة. لا يعتبر تنفيذ عصر مزدوج جهازًا واحدًا مسموحًا. إنه جوهر حديث غير حكومي إلى جانب مُعدل قديم مع اتخاذ قرار تحديد صريح قبل تشغيل أي من المصفحات.

لا يمنع أي من المعاني حالة التطبيق الدائمة. يمكن أن يعيش تدفق العمل أو المهمة أو مسودة خلف مسدس غير شفاف في متجر مشترك. يقوم العميل بإرسال هذا المسدس كإدخال عادي ، وتصديق كل نسخة وترخيص استخدامها. لا يجب أن يسرق سياق البروتوكول إلى ذلك المتجر كبديل للجلسة المزودة.

```figure
mcp-tool-call
```

## استخدمها

`code/main.py` يُبني ويقوم بتصديق ويتبع ويرسل MCP الرسائل بدون إطار.

```bash
python3 code/main.py
python3 -m unittest discover code/tests -v
```

انتبه لثلاثة مستحيلات في الخروج:

- كل طلب يكرر `_meta` الحقول
- كل نتيجة ناجحة `resultType: "complete"` وتشمل هوية الخادم.
- يتم ترتيب نتيجة القائمة بشكل محدد ولديها إشارات مخزن مخزن صريحة.

## أرسله

هذه الدروس السفينة `outputs/skill-mcp-handshake-tracer.md`. يظل اسم الملف التاريخي مستقراً، لكن الفن أصبح الآن متابعاً لطلبات بلا بيان، فإنه يُدقق كل رسالة بشكل مستقل ويعدّب حركة المرور القديمة فقط عندما تكون موجودة حقًا.

## التمارين

1. تغيير نسخة بروتوكول طلب واحد إلى `2027-01-01`تأكد من أن رمز الخطأ `-32022` والبيانات تعلن عن النسخة المدعومة.
2. إزالة `io.modelcontextprotocol/clientCapabilities` تأكد من أن الخادم لا يستخدم إمكانات الطلب الأول.
3. قم بإعكس سجل أداة في الذاكرة. تأكيد `tools/list` لا يزال يعود نفس الترتيب التحديدي.
4. التغيير `cacheScope` من `public` إلى `private`- شرح السياقات التي يمكن إعادة استخدام الاستجابة في كل حالة.
5. إضافة اختيارية `clientInfo` اختبار التخلي عن المعلومات. يجب أن تظل الطلب صالحاً لأن هوية العميل توصي بها وليس مطلوبة.

## الشروط الرئيسية

| المدة | المعنى |
|------|---------|
| بروتوكول بدون جنسية | كل طلب يقدم البيانات المعدنية اللازمة لتفسيرها |
| طلب البيانات المعدنية | الإصدار، قدرات العميل، والهوية الموصى بها للعميل في `params._meta` |
| `server/discover` | طريقة الخادم الإلزامية للنسخ والقدرات والإرشادات والهوية |
| `resultType` | التمييز على كل نتيجة حديثة ناجحة |
| النتيجة القابلة للتخفيض | النتيجة التي تشمل المطلوبة `ttlMs` و `cacheScope` إشارات |
| عصر البروتوكول | البيانات المعدنية الحديثة حسب الطلب أو التبني المحدد من خلال اتصال سابق |
| مدة حياة النقل | عمر العملية أو الاتصال أو تدفق الاستجابة ، وليس حالة جلسة البروتوكول |
| `-32022` | خطأ في إصدار البروتوكول غير المدعوم مع الإصدارات المطلوبة والدعومة |

## المزيد من القراءة

- [MCP الهندسة المعمارية](https://modelcontextprotocol.io/specification/2026-07-28/architecture)
- [MCP البروتوكول الأساسي](https://modelcontextprotocol.io/specification/2026-07-28/basic)
- [MCP اكتشاف الخادم](https://modelcontextprotocol.io/specification/2026-07-28/server/discover)
- [MCP 2026-07-28 تاريخ التغيير](https://modelcontextprotocol.io/specification/2026-07-28/changelog)
