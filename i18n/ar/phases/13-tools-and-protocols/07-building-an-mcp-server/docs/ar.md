# بناء MCP الخادم: بدون جنسية Python و TypeScript

> -أحد الأحدث MCP لا يتذكر الخادم ضغط اليد. يؤكد البيانات المعدنية على كل طلب ، ويقوم بتشغيل عامل واحد ، ويرد نتيجة واحدة من النتائج.

**Type:** Build
**Languages:** Python, TypeScript
**Prerequisites:** Phase 13, Lesson 06
**Time:** ~85 minutes

## أهداف التعلم

- التنفيذ إلزامي `server/discover` لـ MCP `2026-07-28`.
- تأكيد إصدار البروتوكول وإمكانيات العميل على كل طلب.
- تعرض الأدوات والموارد والطلبات بتنظيم القائمة المحددة.
- العودة `resultType`، هوية الخادم، وتلميحات التخزين على النتائج الصحيحة.
- خدمة نفس العقد بدون ولاية على استديو جديد Python و TypeScript.

## المشكلة

خادم يخزن قدرات العميل بعد الرسالة الأولى سهلة البناء وصعبة التشغيل. قد تخدم نفس العملية العملاء المتسلسلين. قد يصل طلب بعيد إلى عامل مختلف. يمكن أن تسرب إعلان قدرة قديمة السلوك عبر حدود التفويض.

MCP `2026-07-28` يحل بروتوكول جزء من هذه المشكلة عن طريق جعل كل طلب وصف الذاتي. لا يزال تطبيقك يمكن أن تحتفظ بقوائم دائمة، وظائف، أو التعاملات الحالة الصريحة. ما لا يمكن أن تحتفظ به هو حالة بروتوكول مخفية التي تغير كيفية فك طلب لاحقا.

هذه الدروس تقوم ببناء خادم ملاحظات مرتين Python و TypeScript الإصدارات تستخدم فقط مكتباتها القياسية للبرنامج الأساسي. كلاهما يعرض نفس الطرق ويتم تنفيذ نفس العقد السلكي.

## المفهوم

### حلقة الرسائل الحديثة

```text
read one JSON-RPC line
parse the envelope
if it is a notification, do not respond
validate params._meta for this request
route by method
wrap success with resultType and serverInfo
write one JSON-RPC response line
forget request-scoped metadata
```

ثلاثة قواعد استوديو لا تزال مهمة:

- اكتب فقط JSON-RPC رسائل إلى (ستودوت) ، أرسل التشخيص إلى (ستودر)
- حدد الرسائل مع خط جديد وفرز كل رد
- اخرج بسرعة عندما يصل الـ (ستدين) EOF.

حياة العملية هي حياة النقل. MCP جلسة

### التحقق من التحقق من التحقق من التحقق من التحقق

كل طلب يجب أن يكون:

```json
{
  "params": {
    "_meta": {
      "io.modelcontextprotocol/protocolVersion": "2026-07-28",
      "io.modelcontextprotocol/clientCapabilities": {},
      "io.modelcontextprotocol/clientInfo": {
        "name": "notes-client",
        "version": "1.0.0"
      }
    }
  }
}
```

الحقول الأولى والثانية مطلوبة. `clientInfo` يوصى به. تأكيد شكل هوية موجود، ولكن لا تعامله كتحقق.

إذا كان الإصدار غير مدعوم، رمز العودة `-32022` مع `requested` و `supported`. المعلومات المفقودة عن الطلب غير صالحة `-32602`لا تملأ أبداً الحقول المفقودة من مكالمة سابقة

### الاكتشاف المفروض

يجب أن تنفيذ الخوادم الحديثة `server/discover`. نتيجة اكتشاف كاملة تشمل الإصدارات الحديثة المدعومة والإمكانيات والإرشادات الاختيارية ، وتلميحات التخزين الآلي ، و هوية الخادم في النتيجة `_meta`:

```json
{
  "resultType": "complete",
  "supportedVersions": ["2026-07-28"],
  "capabilities": {
    "tools": {"listChanged": false},
    "resources": {"listChanged": false, "subscribe": false},
    "prompts": {"listChanged": false}
  },
  "ttlMs": 3600000,
  "cacheScope": "public",
  "_meta": {
    "io.modelcontextprotocol/serverInfo": {
      "name": "notes-server",
      "version": "2.0.0"
    }
  }
}
```

لا يفتح "ديسكفري" الخادم، قد يطلب العميل `tools/list` دون أن نطلق على اكتشاف لأن `tools/list` يحتوي بالفعل على نفس البيانات المعدنية للمطالبة.

### الأدوات

`tools/list` يعود قائمة تحديدية من وصفات الأدوات. يحسن ترتيب الاستجابة من تخزين الاستجابة ويبقي سياق النموذج مستقراً. `ttlMs` و `cacheScope`.

`tools/call` يعيد كتلة المحتوى و `isError`استخدم JSON-RPC خطأ عندما تكون غلاف البروتوكول أو معايير الطريقة غير صالحة. `isError: true` عندما يتم تشغيل دعوة أداة صالحة ولكن الأداة نفسها تفشل.

تعليقات الأدوات لا تزال إشارات، وليس إجراءات:

- `readOnlyHint`
- `destructiveHint`
- `idempotentHint`
- `openWorldHint`

يجب على المضيف استخدامها للتأكيد والعرض. يجب على الخادم لا يزال ينفذ الإذن الحقيقي.

### الموارد

`resources/list` يعود مستقر URI وصفيات `resources/read` يعود المحتوى المكتوب. كليهما قابلة للتخفيض في `2026-07-28`، لذا كلاهما يشمل `ttlMs` و `cacheScope`.

الاستخدام `cacheScope: "private"` لا يجوز لإعادة استخدام الاحتياطي المشترك للردود الخاصة عبر سياقات الائتمان.

التغيير الحديث لا يستخدم `resources/subscribe`- العميل يفتح `subscriptions/listen` و الطلبات `resourceSubscriptions` أو فئات تغيير القائمة. الدروس 10 تبني هذا التدفق.

### الإشارات

`prompts/list` هو قابلة للتخفيض و تحديد. `prompts/get` يعطي عرضًا مسمّىً مع حجج. النتيجة المسمّمة للمساعدة كاملة، ولكنها ليست من القائمة القابلة للتخزين أو نتائج القراءة التي تتطلب إشارات التخزين.

### كل نتيجة ناجحة يتم كتابتها

تستخدم الأمثلة لفاحة واحدة لكل نجاح:

```python
def complete(payload):
    return {
        "resultType": "complete",
        **payload,
        "_meta": {SERVER_INFO_KEY: SERVER_INFO},
    }
```

قائمة، قراءة، ومعالجة اكتشافات إضافة `ttlMs` بالإضافة `cacheScope`. مركزية هذه الملفات تمنع أحد المستخدمين من حذف صامتة حقل النتائج الحديثة.

### لا يوجد طلبات من الخادم

يمكن للخادم الحديث إرسال الإشعارات المتعلقة بطلب العميل، أو الإشعارات على خادم مفتوح `subscriptions/listen` لا يجب أن ترسل JSON-RPC طلب

عندما يحتاج عامل إلى أخذ العينات أو إثارة أو إدخال الجذور ، فإنه يعيد `input_required` النتيجة. يقوم العميل بتلبية طلبات المدخلات المضمنة ومحاولة الطريقة الأصلية مع معرف طلب جديد. يتناول الدروس 11 نمط طلبات متعددة رحلة.

### التوافق الصريح مع التراث

يمكن أن يقوم خادم عصر مزدوج أيضا بتنفيذ `2025-11-25` يضغط على فرع إرث منفصل بشكل واضح. فإنه يختار السلوك الحديث عندما يتطلب الحديث `_meta` الحقول موجودة وتتبع السلوك عندما تتلقى `initialize`.

لا تضع `2026-07-28` الطلب من خلال المسار التمسك اليدوي التراثي. لا تملأ الحديث `resultType` النتائج التبني المترتبة على التبني المترتبة على النتائج المترتبة على التبني المترتبة على التبني المترتبة على النتائج المترتبة على التبني المترتبة على النتائج المترتبة على التبني المترتبة على النتائج المترتبة على التبني المترتبة على النتائج المترتبة على النتائج المترتبة على النتائج المترتبة على النتائج المترتبة على النتائج المترتبة على النتائج المترتبة على النتائج المترتبة على النتائج المترتبة على النتائج المترتبة على النتائج المترتبة على النتائج المترتبة على النتائج المترتبة على النتائج المترتبة على النتائج المترتبة على النتائج المترتبة على النتائج المترتبة على النتائج المترتبة على النتائج المترتبة على النتائج المترتبة على النتائج المترتبة على النتبة

```figure
t3-dispatch-loop
```

## استخدمها

أطلقوا Python التجربة والمحاكمات المحدودة للخادم:

```bash
cd code
python3 main.py --demo
python3 -m unittest discover tests -v
```

أطلقوا TypeScript الميناء مع TypeScript المتجاوز:

```bash
npx tsx main.ts --demo
```

الظهور يرسله `server/discover`يردّ كل طلب حديث بيانات أساسية. كل نجاح يتضمن هوية الخادم.

## أرسله

هذه الدروس السفينة `outputs/skill-mcp-server-scaffolder.md`. إنّه ينتج خطة خادم حديثة مع عقد اكتشاف، وتصديق حسب الطلب، وقوائم تحديدية قابلة للتخزين، ومعدّل إرث معزول اختياري.

## التمارين

1. إزالة القدرات من طلب واحد وإثبات أن الخادم لا يستخدم إعلان الطلب السابق مرة أخرى.
2. عكس `TOOLS`, `PROMPTS`تأكد من أن جميع نتائج القائمة تظل مستقرة
3. إضافة مدمرة `notes_delete` أداة ويتطلب التحقق من الإذن داخل المنفذ. `destructiveHint` كـ UX فقط إشارة
4. إضافة `resources/templates/list` مع `ttlMs`, `cacheScope`، و التنظيم المحدد
5. قم ببناء مُعدل إرث منفصل لـ `2025-11-25`إضافة اختبارات تثبت أن طلب حديث لا يدخل

## الشروط الرئيسية

| المدة | المعنى |
|------|---------|
| خادم بلا جنسية | يتعامل مع كل طلب من بياناتها المعدنية الخاصة دون ذاكرة جلسة البروتوكول |
| `server/discover` | طريقة حديثة إلزامية تعلن عن الإصدارات والقدرات |
| النتيجة الكاملة | النتيجة الحديثة الناجحة مع `resultType: "complete"` |
| النتيجة القابلة للتخفيض | النتيجة التي تم اكتشافها أو القائمة أو قراءة الموارد مع `ttlMs` و `cacheScope` |
| قائمة تحديديات | نفس السجل المنطقي ينتج نفس ترتيب البنود |
| هوية الخادم | الموصى بها `io.modelcontextprotocol/serverInfo` نتيجة لذلك `_meta` |
| خطأ في الأداة | دعوة أداة صالحة تعيد المحتوى مع `isError: true` |
| خطأ بروتوكول | غير صالحة JSON-RPC أو MCP طلب أعيد عبر `error` |

## المزيد من القراءة

- [MCP المواصفات 2026-07-28](https://modelcontextprotocol.io/specification/2026-07-28/)
- [MCP اكتشاف الخادم](https://modelcontextprotocol.io/specification/2026-07-28/server/discover)
- [MCP الأدوات](https://modelcontextprotocol.io/specification/2026-07-28/server/tools)
- [MCP الموارد](https://modelcontextprotocol.io/specification/2026-07-28/server/resources)
- [MCP الإشارات](https://modelcontextprotocol.io/specification/2026-07-28/server/prompts)
- [MCP استديو النقل](https://modelcontextprotocol.io/specification/2026-07-28/basic/transports/stdio)
