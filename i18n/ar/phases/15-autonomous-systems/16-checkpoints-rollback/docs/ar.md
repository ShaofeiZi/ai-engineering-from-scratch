# نقاط التفتيش والعودة

> تتواصل كل انتقال من حالة الرسم البياني. عندما ينهار عامل، ينتهي عقده الإيجار ويأخذ عامل آخر في آخر نقطة تفتيش. تحتفظ Cloudflare Durable Objects بالحالة على مدار ساعات أو أسابيع. يحدد تقديم المقترحات ثم التزام (الدرس 15) خطة إعادة التنفيذ لكل عمل. يغلق التحقق من بعد العمل الحلقة. EU AI قانون المادة 14 يجعل الإشراف البشري الفعال إلزامياً للأنظمة ذات المخاطر العالية  هذا يعني في الممارسة العملية أن نقاط التفتيش يجب أن تكون قابلة للتحقيق، يجب أن يتم التدريب على عمليات الإعادة إلى العمل، و يجب أن تتجاوز مسار المراجعة الانتشار. وضع الفشل الحاد: بدون أزرار الاختفاء والتحقق من الشروط المسبقة، يمكن لإعادة المحاولة بعد الفشل المؤقت إضافة إثنين إلى إجراء تمت الموافقة عليه بالفعل. التحقق بعد العمل هو ما يلتقطه.

**Type:** Learn
**Languages:** Python (stdlib, checkpoint and rollback state machine)
**Prerequisites:** Phase 15 · 12 (Durable execution), Phase 15 · 15 (Propose-then-commit)
**Time:** ~60 minutes

## المشكلة

إن التنفيذ الدائم (درس 12) يجعل العميل المتعثر قابلاً لإعادة التنفيذ. تقديم العرض ثم التزام (درس 15) يجعل الإجراء المعتمد قابلاً للتدقيق. يضم هذا الدروس لهم: ماذا يحدث عندما يتم تنفيذ الإجراء المعتمد جزئياً ، يتعثر ، ويتم إعادة التنفيذ؟ متى يتم إعادة التنفيذ ، ومع أي حالة؟

الأنظمة الحقيقية تُحكم هذا بشكل مختلف:

- **LangGraph** نقاط التفتيش كل انتقال من الرسم البياني إلى PostgreSQL. في حالة حدوث حادث للعمال، يتم إطلاق العقد الإيجاري ويتم استئناف عامل آخر في آخر نقطة تفتيش. `interrupt()`، والتي نفسها تستمر
- **Cloudflare الأجسام الدائمة** تحافظ على حالة المفتاح على مدار ساعات أو أسابيع. تحفّز الحساب مع التخزين للعمل المعتمد.
- **Microsoft Agent Framework** المكشف `Checkpoint` الأسباب الأولية في سير العمل API؛ إعادة التشغيل بالإضافة إلى عدم القدرة على التشغيل تغطي محاولات إعادة التشغيل.

في كل حالة، الجمع الذي يعمل فعلا هو: مفتاح الاختلاف (منع إنجاز مزدوج) + التحقق من الشروط المسبقة (الوضع ما زال ما أقرنا ضد) + التحقق من بعد العمل (تحدثت الآثار الجانبية بالفعل) + إعادة التحقق من الفشل.

## المفهوم

### كل انتقال يستمر

التحول إلى حالة الرسم البياني هو أي خطوة تحرك تدفق العمل من حالة واحدة إلى أخرى. لا تزال تنفيذات البغاءات قائمة إلا في نقاط تعيين محددة. لا تزال تنفيذات الإنتاج قائمة في كل انتقال. التكلفة (بعض الكتابة الإضافية) صغيرة نسبياً إلى مكاسب الاعتماد (تراجع الأرض في أي مكان ، وتحقيق استرداد الإيجار).

### إعادة تأجير

عندما يقع عامل، لا يفقد سير العمل؛ ينفذ العقد (الادعاء القصير على أن هذا العامل يقوم بتنفيذ هذا الجري) ببساطة. يقوم عامل آخر بتحديد آخر نقطة تفتيش وتستأنف. آلية الإيجار هي ما يسمح لنظم الإنتاج بالبقاء على قيد الحياة في الانتشارات المتحركة دون فقدان العمل أثناء الطيران.

### الجهد المفروض بالإضافة إلى الشروط المسبقة

إنّ الجهد وحده ليس كافياً. فكر في هذا: يتم الموافقة على عملية عمل "تحويل 100 دولار من A إلى B عندما يكون الميزان > 1000 دولار". يتم التزام عملية العمل ، وتسقط في منتصف التنفيذ ، وتستأنف. إذا تم التحقق من مفتاح الاختلاف فقط ، واستئناف التنفيذ ، يتم تنفيذ النقل مرة واحدة (صحيحة). لكن فكر في أن بين الإعاقة والإستئناف، تراجع موازنة (أ) إلى 500 دولار عبر تدفق عمل مختلف. لا يزال التحقق من عدم القدرة على التصرف يمر، ولكن الشرط المسبق لا يمر. بدون تحقق من الشروط المسبقة، نحن نُرسل إضافية.

كل عمل يتطلب كلتا:

- **مفتاح عدم القدرة**: يمنع إنجاز المزدوج.
- **التحقق من الحالة السابقة**: يؤكد أن الدولة لا تزال متوافقة مع ما تمت الموافقة عليه.

### التحقق بعد الإجراء

"الأداة التي عادت 200" ليست التحقق. التحقق الحقيقي يقرأ مرة أخرى الحالة المستهدفة ويؤكد تأثير الجانب حدث فعلا.

- تحديث قاعدة البيانات: `UPDATE ... RETURNING *` ثم تأكد من حالة تطابق الصف المرجع المقصودة.
- إرسال البريد الإلكتروني: تحقق من المجلد المرسل للإرسال ID بعد الإستسلام
- كتابة الملف: قراءة الملف مرة أخرى وتشويقها.
- API المكالمة: المتابعة `GET` على الموارد المستهدفة.

إذا فشلت التحقق، فإن تدفق العمل في حالة سيئة معروفة.

### خطط الرد

كل إجراء متواصل في تقديم المقترحات ثم التزام (الدرس 15) يحمل خطة إعادة التنفيذ.

- **الرد الخلفي داخل الفرقة**: عكس الآثار الجانبية مباشرة (`DELETE` بعد `INSERT`, `Send-correction-email` بعد إرسالها).
- **المعاملة المكافئة**: عمل جديد يُحبط الأصلي (المعياري) SAGA النمط)
- **التراجع خارج المجموعة**: تحذير الإنسان، توقف سير العمل، ترك الحالة السيئة للتحقيق.

يجب أن يذكر في الاقتراح عدم إعادة التنفيذ ("لا يمكننا إلغاء هذا") HITL في وقت التزام (المدرس 15 التحدي والرد).

### EU AI القانون المادة 14 القراءة التشغيلية

تتطلب المادة 14 "إشراف بشري فعال" على الأنظمة ذات المخاطر العالية.

- نقاط التفتيش قابلة للتحقيق من قبل المراجعة.
- يتم تدريب الردود (تم اختبارها من نهاية إلى آخر مرة واحدة على الأقل).
- تتبقى مسار المراجعة على الانتشار (الخلفية لم تكن مؤقتة).
- يتم إشعار التحققات الفاشلة، وليس تسجيلها بصمت.

سير العمل الذي يتوقف في منتصف الالتزام، ويستأنف، ويؤدي إلى إكمال التأثير الجانبي دون مسار التحقق + الردع لا ينجو من اختبار المادة 14.

### وضع الفشل الحاد: التنفيذ المزدوج

أكثر حوادث الإنتاج شيوعاً في هذا المجال:

1. تمت الموافقة على العمل، مفتاح الإستثمار
2. الالتزام يبدأ، ينفذ، يعيد 200.
3. تدفق العمل يتعطل قبل أن تستمر حالة "التزام".
4. سير العمل يستمر؛ يرى "موافق ولكن ليس ملتزم"؛ يعيد تنفيذها.
5. تأثير الجانب يطلق مرتين

التخفيف: استمر في إعداد "في الطيران" قبل تنفيذها، تنفيذها بمفتاح إعفاء، ثم علامة "ملتزم" فقط بعد نجاح التحقق من بعد الإجراء. إذا فشلت إطلاق النار الإجراءية وتكشف الحالة، تعرف على التحقق وإعادة إطلاق النار (إذا لزم الأمر). إذا نجحت إطلاق الحالة وتفشل الإجراء، يمكنك التحقق من وإطلاق النار مرة واحدة بالضبط عبر مسار الاسترداد.

```figure
checkpoint-replay
```

## استخدمها

`code/main.py` يقوم السائق بتنظيم عملية التدريب المحددة مع إمكانية الإصلاح، والشروط المسبقة، والتحقق، والإعادة التدريب. يحاكي السائق أربعة سيناريوهات: الجري النظيف، والحاولات المجددة بعد الحادث (التقاط إمكانية الإصلاح) ، وفشل الإصلاح (إجراءات إزالة التدريب دون إطلاق النار) ، والتحقق من الفشل (إطلاق إعادة التدريب).

## أرسله

`outputs/skill-rollback-rehearsal.md` يقوم بتصميم اختبار التجربة التدريبية للجريان العمل المقترح والتحقق من الخلفية في نقطة التفتيش لتحقيق استمرارية مسار التدقيق.

## التمارين

1. أركض `code/main.py`تأكد من أنّه في حالة الحادث أثناء الالتزام، يتمّ إطلاق النار مرة واحدة بالضبط في كلّ محاولة إعادة.

2. تعديل نمط "علامة كما تم أولاً، ثم القيام به" بحيث يكتب الحالة حرائق بعد الإجراء. إعادة عرض سيناريو الحطام. قياس عدد الإجراءات المكررة النار.

3. صياغة خطة إعادة التأثير لفعلية إنتاجية محددة (مثل "رسل إلى قناة Slack"). تصنيف كمنطقة داخل المجموعة، أو تعويض، أو خارج المجموعة. توجيه الاختيار.

4. خذ تدفق عمل واحد تعرفه. حدد كل انتقال حالة. حدد كل واحد مع متطلبات استدامة (استمر / لا تستمر). عد تلك التي لا تستمر حاليا.

5. اختبار التردد المتكرر: تصميم اختبار من نهاية إلى نهاية يقوم بتشغيل تدفق عمل حقيقي، ويصطدم به، و يؤكد إطلاق مسار التردد. ما الذي يؤكد عليه الاختبار؟

## الشروط الرئيسية

| المدة | ما يقوله الناس | ما يعنيه هذا في الواقع |
|---|---|---|
| نقطة تفتيش | "نقطة حفظ" | كل انتقال من حالة الرسم البياني يستمر إلى متجر دائم |
| الإيجار | "دعوى العمال" | الادعاء القصير الذي يظهر أن العامل يقوم بعمل ركض؛ ينتهي صلاحيته عند الحادث |
| الشروط المسبقة | "بوابة الدولة" | التأكيد على أن الدولة لا تزال متسقة مع الإجراء المعتمد |
| التحقق بعد الإجراء | "تعدي قراءة الشيك" | تأكيد التأثير الجانبي الذي حدث في نظام الهدف |
| الرد الخلفي داخل الفرقة | "إلغاء مباشرة" | عكس التأثير الجانبي مع العملية العكسية |
| المعاملة المكافئة | "SAGA "فقط" | عمل جديد يُحبط الأصلي |
| -أول ما تمّ إعلانه | "وضع كتابة النظام" | استمر في وضع الالتزام قبل العودة من الالتزام |
| المادة 14 | "EU AI "تصرفوا على الإشراف البشري" | عمليات: نقاط تفتيش قابلة للتحقيق، عمليات إعادة التدريب، مسار يمكن مراجعته |

## المزيد من القراءة

- [إطار عملاء مايكروسوفت  التفتيش HITL](https://learn.microsoft.com/en-us/agent-framework/workflows/human-in-the-loop) البدائية في نقاط التفتيش وتعافي الإيجار
- [عملاء Cloudflare  البشر في الحلقة](https://developers.cloudflare.com/agents/concepts/human-in-the-loop/) الأشياء الدائمة كقاع الدولة.
- [EU AI القانون  المادة 14: الإشراف البشري](https://artificialintelligenceact.eu/article/14/) نقطة أساسية للتنظيم
- [Anthropic قياس استقلالية الوكالة في الممارسة العملية](https://www.anthropic.com/research/measuring-agent-autonomy) إطار موثوقية لأجليات العمل على الأفق الطويل.
- [Anthropic — Claude وكيل الرمز SDK: حلقة العميل](https://code.claude.com/docs/en/agent-sdk/agent-loop) شكل سير العمل Claude أرقام روتينية
